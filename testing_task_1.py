class CardGame:

  def check_for_ace(self, card):
    if card.value = 1: #There should be a double equals here to check equivalency

      return True
    else #needs a colon here
      return False
   

  dif highest_card(self, card1 card2): #def is misspelled as 'dif' and there is not comma between card1 and card2
  if card1.value > card2.value: #indentation erro
    return card #should be card1
  else:
    return card2 #test does not check for 2 cards being equal
  


def cards_total(self, cards):
  total #need to set total to be equal to something.
  for card in cards:
    total += card.value
    return "You have a total of" + total #this will be an error as this will try to concatenate
    #a string and an integer.
    #This return needs to be indented back to the left as you only want the final total to be returned
  
```
